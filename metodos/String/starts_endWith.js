const StartsWith = 'lucas Manassés';
const resultado = StartsWith.startsWith('luc'); 
console.log(resultado);
// verifica se a varivel está atendendo a condição de ser Chamada dok jeito que foi determinado 

const EndWith = 'Lucas Manasses';
const res= EndWith.endsWith('asses'); // ele busca como termina a sua string. vai procurar se o que foi passado, vai atender a condição 
console.log(res);