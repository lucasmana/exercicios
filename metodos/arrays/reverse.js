let array = [1, 4, 3, 42, 5, 90, 7, 8, 9, 10];
let novoArray = array.reverse()
console.log(novoArray);
//Esse metodo inverte a ordem dos elementos do array.
//No exemplo acima, ele inverte a ordem dos números no array original, resultando em
// ele muda a ordem do array e não a posição dos numeros que estao no array.

