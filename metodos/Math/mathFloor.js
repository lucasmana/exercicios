const number = 7;
const number1= 8.94
const resultado = number + number1;
console.log(`Este é o resultado sem arredndar ${resultado}`);
console.log(`Este é o resultado arredondado`, Math.floor(resultado));
//O método math é utilazado na saída, ou seja, no resultad, quando o for ser aplicado. exemplo: console.log(Math.floor());
//arredonda para baixo;