const number = 7.7;
const number1= 8.34
const resultado = number + number1;
console.log(`Este é o resultado sem arredndar ${resultado}`);
console.log(`Este é o resultado arredondado`, Math.round(resultado));
//O método math é utilazado na saída, ou seja, no resultad, quando o for ser aplicado. exemplo: console.log(Math.round());